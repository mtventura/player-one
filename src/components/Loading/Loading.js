import LogoLoadingScreen from '../../assets/images/LogoLoadingScreen.png'
const Loading = ({className}) => {
    return (
        <img className={className} alt="" src={LogoLoadingScreen}/>
    )
}

export default Loading